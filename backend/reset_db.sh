#!/bin/bash
# Скрипт для быстрого пересоздания базы данных в режиме разработки

echo "🗑️  Удаляем старую базу данных..."

# Переходим в корень репозитория (на уровень выше backend)
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(dirname "$SCRIPT_DIR")"

# Путь к БД относительно корня репозитория
DB_PATH="$REPO_ROOT/data/tasks.db"

if [ -f "$DB_PATH" ]; then
    echo "📦 Создаем резервную копию: ${DB_PATH}.backup"
    cp "$DB_PATH" "${DB_PATH}.backup"

    echo "🗑️  Удаляем: $DB_PATH"
    rm -f "$DB_PATH"
    echo "✅ База данных удалена"
else
    echo "ℹ️  База данных не найдена по пути: $DB_PATH"
fi

echo ""
echo "✅ Готово! Запустите приложение - база данных создастся автоматически."
echo ""
echo "Для запуска:"
echo "  docker-compose up -d              # с Docker"
echo "  uvicorn app.main:app --reload     # локально из папки backend"
