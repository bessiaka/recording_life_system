# Интеграция с Voice App

## 📋 Как подключить голосовые команды для TODO

### Шаг 1: Скопировать файл команд

Скопируй `todo_commands.py` в директорию команд твоего voice-app:

```bash
cp voice-integration/commands/todo_commands.py \
   ~/WORKSPACE/WORKSPACE_LOCAL_GITLAB/VoskDemoMicroCheck/src/commands/
```

### Шаг 2: Обновить config/commands.yaml

Добавь содержимое `voice-integration/config/todo_commands.yaml` в твой `config/commands.yaml`:

```yaml
# config/commands.yaml

wake_word: "команда"
command_timeout: 3.0

commands:
  system:
    диагностика:
      action: "CMD_DIAGNOSTICS"
      # ... существующие команды
  
  # ⬇️ ДОБАВЬ ЭТО:
  todo:
    добавить:
      action: "CMD_TODO_ADD"
      description: "Добавить задачу"
      handler: "todo_commands.AddTaskCommand"
      params:
        api_url: "http://localhost:8000"
        default_priority: 999
    
    удалить:
      action: "CMD_TODO_DELETE"
      description: "Удалить задачу"
      handler: "todo_commands.DeleteTaskCommand"
      params:
        api_url: "http://localhost:8000"
    
    список:
      action: "CMD_TODO_LIST"
      description: "Показать список задач"
      handler: "todo_commands.ListTasksCommand"
      params:
        api_url: "http://localhost:8000"
```

### Шаг 3: Установить зависимость requests

Добавь в `requirements.txt`:

```
requests>=2.31.0
```

И переустанови зависимости:

```bash
docker-compose build voice-app
```

### Шаг 4: Обновить CommandProcessor

В `src/core/command_processor.py` нужно передавать распознанный текст в команду:

```python
# Было:
result = command.run()

# Должно быть:
result = command.run(recognized_text=text)
```

И в `src/commands/base.py`:

```python
def run(self, recognized_text: str = "") -> CommandResult:
    """Запуск команды с текстом"""
    # ...
    result = self.execute(recognized_text=recognized_text)
    # ...
```

А в каждой команде метод `execute`:

```python
def execute(self, recognized_text: str = "") -> CommandResult:
    # Теперь команда имеет доступ к полному тексту
    pass
```

## 🎤 Примеры использования

После интеграции можно будет говорить:

### Добавить задачу
```
👤 "команда добавить задачу купить молоко"
🤖 ✅ Задача добавлена: купить молоко

👤 "команда добавить позвонить маме"
🤖 ✅ Задача добавлена: позвонить маме
```

### Удалить задачу
```
👤 "команда удалить первую"
🤖 ✅ Задача удалена: купить молоко

👤 "команда удалить задачу номер два"
🤖 ✅ Задача удалена: позвонить маме
```

### Список задач
```
👤 "команда список"
🤖 📋 Задач: 3. 1. Купить молоко, 2. Позвонить маме, 3. Сделать отчет
```

## 🔧 Настройка

### Изменить URL API

Если backend работает на другом адресе, измени в `config/commands.yaml`:

```yaml
params:
  api_url: "http://192.168.1.100:8000"  # IP твоего сервера
```

### Изменить приоритет по умолчанию

```yaml
params:
  default_priority: 1  # Новые задачи будут в начале списка
```

## 🐛 Отладка

### Проверить доступность API

```bash
curl http://localhost:8000/api/tasks
```

### Логи voice-app

```bash
docker logs -f voice-recognizer
```

Должны появиться строки:

```
✅ Задача создана: 'купить молоко' (ID: 1)
🗑️  Задача удалена: 'купить молоко' (ID: 1)
📋 Получен список из 3 задач
```

## 📝 Заметки

- Команды работают только при запущенном backend (`docker-compose up` в todo-voice-app)
- WebSocket синхронизация происходит автоматически - все устройства обновятся
- Можно добавлять задачи голосом, а редактировать в браузере
