# 🧪 Инструкция по проверке Workflow

## 📋 Как работают GitHub Actions / Forgejo Actions

### Важные концепции:

1. **Workflow файлы читаются из той же ветки, где произошло событие**
   ```
   Push в main → используется .forgejo/workflows/deploy.yml из main
   Push в feature → используется .forgejo/workflows/deploy.yml из feature
   ```

2. **Trigger условия определяют, когда запускается workflow**
   ```yaml
   on:
     push:
       branches: [main]  # Запустится ТОЛЬКО при push в main
   ```

3. **Текущая ситуация:**
   - Workflow находится в ветке `claude/setup-github-actions-cicd-F5yxE`
   - Production workflow настроен на trigger только для `main`
   - **Push в `claude/setup...` НЕ запустит production workflow**
   - Нужно создать тестовый workflow для проверки

---

## ✅ Пошаговая инструкция для Desktop

### Шаг 1: Получить последние изменения (тестовый workflow)

```bash
cd ~/WORKSPACE/WORKSPACE_LOCAL_GITLAB/todo-voice-app

# Получить тестовый workflow
git pull origin claude/setup-github-actions-cicd-F5yxE
```

### Шаг 2: Закоммитить ваши изменения в README.md

```bash
# Проверить статус
git status

# Если README.md в staged (Changes to be committed):
git commit -m "docs: обновление README с инструкциями по запуску"

# Если README.md не в staged:
git add README.md
git commit -m "docs: обновление README с инструкциями по запуску"
```

### Шаг 3: Добавить тестовый workflow

```bash
# Добавить новый тестовый файл
git add .forgejo/workflows/test-deploy.yml

# Закоммитить
git commit -m "test: добавлен тестовый workflow для проверки CI/CD"
```

### Шаг 4: Push и проверка запуска

```bash
# Push в ветку
git push -u origin claude/setup-github-actions-cicd-F5yxE
```

**⚠️ ВАЖНО:** Если у вас настроен Forgejo runner на Server, workflow запустится автоматически!

### Шаг 5: Проверка логов Forgejo Runner на Server

**На Server (192.168.1.72):**

```bash
# Проверить логи runner
docker logs -f forgejo-runner-alpine

# Вы должны увидеть:
# [INFO] workflow started: Test Deploy Workflow
# [INFO] job started: test-deploy
```

### Шаг 6: Проверка в Forgejo Web UI

1. Откройте Forgejo: `http://192.168.1.72:3001`
2. Перейдите в репозиторий `recording_life_system`
3. Откройте **Actions** → **Runs**
4. Найдите запуск **"Test Deploy Workflow (Temporary)"**
5. Проверьте логи каждого step

**Ожидаемый результат:**
```
✅ Test - Print environment info
✅ Test - Check .env.production exists
✅ Test - Check Docker Compose config
✅ Test - Dry run
✅ Test - Success notification
```

---

## 🔍 Что проверяет тестовый workflow

1. **Информация об окружении**
   - Ветка, коммит, автор, репозиторий

2. **Наличие .env.production**
   - ❌ Если файла нет → workflow упадет с ошибкой
   - ✅ Если файл есть → покажет его содержимое

3. **Docker Compose конфигурация**
   - Проверит валидность docker-compose.yml

4. **Dry run**
   - Не делает реальный деплой
   - Только проверяет, что все работает

---

## 🚀 После успешной проверки

### Вариант 1: Merge в main через PR (рекомендуется)

```bash
# На Desktop
cd ~/WORKSPACE/WORKSPACE_LOCAL_GITLAB/todo-voice-app

# Удалить тестовый workflow
git rm .forgejo/workflows/test-deploy.yml
git commit -m "test: удален тестовый workflow после проверки"
git push origin claude/setup-github-actions-cicd-F5yxE

# Создать PR в Forgejo Web UI
# Или через gh/forgejo CLI:
git checkout main
git merge claude/setup-github-actions-cicd-F5yxE
git push origin main
```

### Вариант 2: Прямой merge в main (быстрый способ)

```bash
# На Desktop
git checkout main
git pull origin main

# Merge ветки
git merge claude/setup-github-actions-cicd-F5yxE

# Удалить тестовый workflow
git rm .forgejo/workflows/test-deploy.yml
git commit -m "test: удален тестовый workflow"

# Push в main
git push origin main
```

**После push в main:**
- Production workflow `.forgejo/workflows/deploy.yml` запустится автоматически
- Проект будет задеплоен на Server с портами 8003 и 3003

---

## 📊 Проверка production деплоя

После merge в main и автоматического деплоя:

```bash
# На Server (192.168.1.72)
docker ps | grep recording_life

# Должны увидеть:
# recording_life_prod-backend
# recording_life_prod-frontend

# Проверить логи
docker logs recording_life_prod-backend
docker logs recording_life_prod-frontend
```

**Проверка в браузере:**
- Frontend: http://192.168.1.72:3003
- Backend API: http://192.168.1.72:8003
- WebSocket: ws://192.168.1.72:8003/ws

---

## ❌ Troubleshooting

### Workflow не запустился после push

**Возможные причины:**

1. **Runner не запущен на Server**
   ```bash
   # На Server
   docker ps | grep forgejo-runner
   docker logs forgejo-runner-alpine
   ```

2. **Runner не зарегистрирован**
   ```bash
   # Проверить в Forgejo Web UI:
   # Settings → Actions → Runners
   # Должен быть виден runner с меткой "alpine"
   ```

3. **Workflow файл содержит ошибки YAML**
   ```bash
   # Проверить синтаксис локально
   cat .forgejo/workflows/test-deploy.yml
   ```

### Workflow упал с ошибкой

**Проверить логи:**
1. Forgejo Web UI → Actions → Runs → Кликнуть на run
2. Посмотреть логи каждого step
3. Найти шаг с ошибкой

**Типичные ошибки:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `.env.production not found` | Файл не создан на Server | Создать `.env.production` в репозитории на Server |
| `port already allocated` | Порт занят | Изменить порты в `.env.production` |
| `docker: command not found` | Docker не доступен в runner | Проверить монтирование `/var/run/docker.sock` |

---

## 📝 Checklist перед production деплоем

- [ ] Тестовый workflow успешно отработал
- [ ] `.env.production` создан на Server с портами 8003/3003
- [ ] Forgejo runner запущен и работает
- [ ] Тестовый workflow удален
- [ ] Ветка замержена в `main`
- [ ] Production workflow запустился после push в main
- [ ] Приложение доступно по http://192.168.1.72:3003

---

## 🎯 Итоговая схема работы

```
┌─────────────────────────────────────────────────┐
│ Desktop (Development)                           │
│ - Разработка в ветке claude/setup...           │
│ - git push → Тестовый workflow запускается      │
│ - Проверка работоспособности                    │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ После успешной проверки:                        │
│ - git merge в main                              │
│ - git push origin main                          │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ Forgejo Runner на Server                        │
│ - Обнаруживает push в main                      │
│ - Запускает Production workflow                 │
│ - Выполняет деплой                              │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ Server (Production)                             │
│ - Приложение запущено на портах 8003/3003      │
│ - Доступно для Phone и Orange Pi               │
└─────────────────────────────────────────────────┘
```
