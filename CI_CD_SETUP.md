# 🚀 CI/CD Pipeline Setup Guide

Автоматический деплой на production сервер через Forgejo Actions.

---

## 📋 Оглавление

1. [Архитектура](#архитектура)
2. [Первичная настройка](#первичная-настройка)
3. [Использование](#использование)
4. [Управление базой данных](#управление-базой-данных)
5. [Troubleshooting](#troubleshooting)

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    РАЗРАБОТКА                           │
│  Desktop (192.168.1.13)                                 │
│  ┌──────────────────────────────────────────┐          │
│  │  1. Разработка и тестирование            │          │
│  │  2. Коммит в ветку                       │          │
│  │  3. Push в Forgejo                       │          │
│  └──────────────┬───────────────────────────┘          │
└─────────────────┼───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│              FORGEJO SERVER                             │
│  Server (192.168.1.72:1080)                            │
│  ┌──────────────────────────────────────────┐          │
│  │  4. Webhook trigger на push в main       │          │
│  │  5. Запуск Forgejo Runner                │          │
│  └──────────────┬───────────────────────────┘          │
└─────────────────┼───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│              DEPLOYMENT                                 │
│  Server (192.168.1.72)                                 │
│  ┌──────────────────────────────────────────┐          │
│  │  6. Checkout кода                        │          │
│  │  7. Применение .env.production           │          │
│  │  8. docker compose down                  │          │
│  │  9. docker compose up -d --build         │          │
│  └──────────────────────────────────────────┘          │
│                                                         │
│  ✅ Приложение доступно:                               │
│     http://192.168.1.72:3003 (frontend)               │
│     http://192.168.1.72:8003 (backend)                │
└─────────────────────────────────────────────────────────┘
```

---

## ⚙️ Первичная настройка

### 1️⃣ На Server (192.168.1.72)

#### Создать .env.production файл

```bash
cd ~/WORKSPACE/recording_life_system

# Создать из примера
cp .env.example .env.production

# Отредактировать значения для production
nano .env.production
```

Пример `.env.production`:

```bash
# Окружение
ENVIRONMENT=production

# Хост конфигурация
HOST_IP=192.168.1.72
HOST_NAME=server

# Порты сервисов
BACKEND_PORT=8003
FRONTEND_PORT=3003

# Backend конфигурация
DATABASE_URL=sqlite:////data/tasks.db
PYTHONUNBUFFERED=1

# Frontend конфигурация
VITE_API_URL=http://192.168.1.72:8003
VITE_WS_URL=ws://192.168.1.72:8003/ws

# Docker конфигурация
COMPOSE_PROJECT_NAME=recording_life_system

# Дополнительные настройки
LOG_LEVEL=info
TZ=Europe/Moscow
```

#### Создать директорию для данных

```bash
mkdir -p ~/WORKSPACE/recording_life_system/data
```

### 2️⃣ Проверка Forgejo Runner

Runner уже должен быть запущен на Server:

```bash
docker ps | grep runner
```

Должен вывести:

```
b8efb4e06b1f   forgejo-runner-optimized:debian   ...   forgejo-runner-optimized
```

Если runner не запущен, запустите его:

```bash
# Команда запуска runner (пример, уточните у вашей документации)
docker run -d \
  --name forgejo-runner-optimized \
  --restart unless-stopped \
  -v /var/run/docker.sock:/var/run/docker.sock \
  forgejo-runner-optimized:debian
```

---

## 🎯 Использование

### Обычный деплой

1. **Разработка на Desktop**

```bash
# Внести изменения в код
git add .
git commit -m "feat: добавлена новая функциональность"
```

2. **Push в main ветку**

```bash
git push origin main
```

3. **Автоматический деплой**

После push в `main` ветку:
- Forgejo автоматически запустит workflow
- Runner выполнит все шаги деплоя
- Через ~2-3 минуты новая версия будет доступна

4. **Проверка статуса**

Откройте в браузере:
```
http://95.165.70.94:1080/bessiaka/recording_life_system/actions
```

### Деплой через Pull Request

1. **Создать PR в Forgejo**

```bash
# На Desktop создать feature ветку
git checkout -b feature/my-new-feature

# Внести изменения
git add .
git commit -m "feat: моя новая фича"

# Push ветки
git push origin feature/my-new-feature
```

2. **Создать Pull Request в Forgejo UI**

- Откройте http://95.165.70.94:1080/bessiaka/recording_life_system
- Нажмите "New Pull Request"
- Выберите `feature/my-new-feature` -> `main`
- Создайте PR

3. **После merge в main - автоматический деплой**

---

## 🗄️ Управление базой данных

### Сохранение данных (по умолчанию)

По умолчанию база данных **сохраняется** при деплое:
- Директория `./data/` не удаляется
- Все задачи остаются после обновления

### Пересоздание базы данных

Если изменилась структура БД (добавлены/удалены столбцы), нужно пересоздать базу.

**Способ 1: Вручную на Server**

```bash
cd ~/WORKSPACE/recording_life_system
rm -f ./data/tasks.db
# При следующем запуске база создастся автоматически
```

**Способ 2: Через коммит с флагом [reset-db]**

```bash
# На Desktop
git add .
git commit -m "feat: изменена структура БД [reset-db]"
git push origin main
```

Workflow автоматически:
1. Обнаружит флаг `[reset-db]` в сообщении коммита
2. Удалит старую базу данных
3. При запуске создаст новую базу

---

## 🔍 Мониторинг и логи

### Просмотр статуса деплоя

**В Forgejo UI:**

```
http://95.165.70.94:1080/bessiaka/recording_life_system/actions
```

### Логи контейнеров на Server

```bash
# Все логи
docker compose -f docker-compose.yml --env-file=.env.production logs -f

# Только backend
docker compose -f docker-compose.yml --env-file=.env.production logs backend -f

# Только frontend
docker compose -f docker-compose.yml --env-file=.env.production logs frontend -f

# Последние 100 строк
docker compose -f docker-compose.yml --env-file=.env.production logs --tail=100
```

### Статус контейнеров

```bash
docker compose -f docker-compose.yml --env-file=.env.production ps
```

---

## 🐛 Troubleshooting

### ❌ Workflow не запускается

**Проверка 1: Runner работает?**

```bash
docker ps | grep runner
```

Если не работает:

```bash
docker start forgejo-runner-optimized
```

**Проверка 2: Webhook настроен?**

В Forgejo:
1. Откройте репозиторий
2. Settings → Webhooks
3. Должен быть активный webhook для Actions

### ❌ Деплой завершился с ошибкой

**Просмотр логов workflow:**

```
http://95.165.70.94:1080/bessiaka/recording_life_system/actions
```

Кликните на failed workflow → посмотрите логи каждого шага

**Типичные проблемы:**

1. **Нет .env.production на сервере**

```bash
# На Server
cd ~/WORKSPACE/recording_life_system
cp .env.example .env.production
# Отредактируйте значения
nano .env.production
```

2. **Docker образы не собираются**

```bash
# Вручную пересобрать на Server
cd ~/WORKSPACE/recording_life_system
docker compose -f docker-compose.yml --env-file=.env.production build --no-cache
```

3. **Порты заняты**

```bash
# Проверить что слушает порты
sudo lsof -i :8003
sudo lsof -i :3003

# Остановить старые контейнеры
docker compose -f docker-compose.yml --env-file=.env.production down
```

### ❌ База данных не создается

```bash
# Проверить права доступа
ls -la ~/WORKSPACE/recording_life_system/data

# Создать директорию если нужно
mkdir -p ~/WORKSPACE/recording_life_system/data
chmod 755 ~/WORKSPACE/recording_life_system/data
```

### ❌ Frontend не подключается к Backend

**Проверка 1: Backend работает?**

```bash
curl http://192.168.1.72:8003/docs
```

Должен вернуть HTML страницу с API документацией

**Проверка 2: Переменные окружения**

```bash
# На Server проверить .env.production
cat .env.production | grep VITE_API_URL
# Должно быть: VITE_API_URL=http://192.168.1.72:8003
```

### ❌ Нужно откатиться на старую версию

```bash
# На Server
cd ~/WORKSPACE/recording_life_system

# Посмотреть последние коммиты
git log --oneline -10

# Откатиться на нужный коммит
git checkout <commit-hash>

# Перезапустить контейнеры
docker compose -f docker-compose.yml --env-file=.env.production up -d --build

# Вернуться на main
git checkout main
```

---

## 📝 Полезные команды

### На Desktop (разработка)

```bash
# Создать .env для разработки
cp .env.example .env.development
ln -s .env.development .env

# Запустить локально
docker compose up -d

# Остановить
docker compose down
```

### На Server (production)

```bash
# Перейти в директорию проекта
cd ~/WORKSPACE/recording_life_system

# Просмотр статуса
docker compose -f docker-compose.yml --env-file=.env.production ps

# Перезапуск всех контейнеров
docker compose -f docker-compose.yml --env-file=.env.production restart

# Перезапуск только backend
docker compose -f docker-compose.yml --env-file=.env.production restart backend

# Остановка
docker compose -f docker-compose.yml --env-file=.env.production down

# Запуск с пересборкой
docker compose -f docker-compose.yml --env-file=.env.production up -d --build

# Очистка всего (включая volumes)
docker compose -f docker-compose.yml --env-file=.env.production down -v
```

---

## 🔐 Безопасность

1. **Никогда не коммитьте .env файлы** (кроме .env.example)
2. **.env.production должен быть только на Server** и не попадать в git
3. **Токен runner** храните в безопасности
4. **Регулярно обновляйте** зависимости проекта

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи workflow в Forgejo
2. Проверьте логи контейнеров на Server
3. Создайте Issue в репозитории с описанием проблемы и логами
