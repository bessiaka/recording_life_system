# CI/CD для recording_life_system

Автоматическое развертывание проекта на продакшн сервер с использованием **GitHub Actions** или **Forgejo Actions**.

## 🎯 Две реализации CI/CD

Проект поддерживает два варианта CI/CD runner:

1. **GitHub Actions Runner** (Ubuntu 20.04) - для GitHub
2. **Forgejo Actions Runner** (Alpine Linux) - для self-hosted Forgejo

## 🚀 Обзор

При каждом слиянии Pull Request в ветку `main` или прямом push в `main`, GitHub Actions автоматически:

1. ✅ Собирает новые Docker образы
2. ✅ Останавливает старые контейнеры
3. ✅ Запускает обновленные контейнеры
4. ✅ Проверяет здоровье сервисов
5. ✅ Очищает старые образы

## 📋 Структура CI/CD

```
.github/
├── workflows/
│   └── deploy.yml                    # GitHub Actions workflow
├── runner/                           # GitHub Actions Runner (Ubuntu)
│   ├── Dockerfile                    # ~700 MB образ
│   ├── docker-compose.yml
│   ├── entrypoint.sh
│   ├── .env.example
│   └── RUNNER_SETUP.md
└── runner-forgejo/                   # Forgejo Actions Runner (Alpine)
    ├── Dockerfile                    # ~85 MB образ
    ├── docker-compose.yml
    ├── entrypoint.sh
    ├── .env.example
    └── RUNNER_SETUP.md

.forgejo/
└── workflows/
    └── deploy.yml                    # Forgejo Actions workflow
```

## 🔍 Сравнение runners

| Характеристика | GitHub (Ubuntu) | Forgejo (Alpine) |
|----------------|-----------------|------------------|
| Базовый образ | ubuntu:20.04 | alpine:latest |
| Размер образа | ~700-1500 MB | ~85-120 MB |
| RAM (idle) | ~150-300 MB | ~50-80 MB |
| RAM (рекомендуется) | 512 MB min | 256 MB min |
| Время старта | ~20-30 сек | ~5-10 сек |
| Совместимость | Максимальная (glibc) | Хорошая (musl) |
| Packages | apt (огромная база) | apk (минималистичная) |
| Use case | Enterprise, GitHub | Personal, Self-hosted |
| Идеально для | Публичных проектов | Домашнего сервера |

## ⚙️ Быстрый старт

### Вариант A: GitHub Actions Runner (Ubuntu)

Подробная инструкция: [.github/runner/RUNNER_SETUP.md](.github/runner/RUNNER_SETUP.md)

**Краткая версия:**

На вашем продакшн сервере (192.168.1.72):

```bash
# Скопируйте файлы из .github/runner на сервер
scp -r .github/runner/* user@192.168.1.72:~/github-runner/

# Подключитесь к серверу
ssh user@192.168.1.72
cd ~/github-runner

# Создайте .env файл с токеном
echo "RUNNER_TOKEN=your_token_here" > .env

# Обновите GITHUB_REPOSITORY в docker-compose.yml
nano docker-compose.yml  # Замените your-username на ваш username

# Запустите runner
docker-compose up -d --build
```

### 2. Получение токена для runner

1. Откройте репозиторий на GitHub
2. Settings → Actions → Runners
3. Нажмите "New self-hosted runner"
4. Скопируйте токен из команды `./config.sh`
5. Добавьте в `.env` файл

### 3. Проверка работы

После настройки runner:

1. Создайте тестовую ветку и сделайте изменение
2. Создайте Pull Request в `main`
3. После merge workflow запустится автоматически
4. Проверьте в Actions → Deploy to Production

---

### Вариант B: Forgejo Actions Runner (Alpine) 🆕

**Рекомендуется для домашнего сервера с ограниченными ресурсами!**

Подробная инструкция: [.github/runner-forgejo/RUNNER_SETUP.md](.github/runner-forgejo/RUNNER_SETUP.md)

**Краткая версия:**

На вашем продакшн сервере (192.168.1.72):

```bash
# Скопируйте файлы на сервер
scp -r .github/runner-forgejo/* user@192.168.1.72:~/forgejo-runner/

# Подключитесь к серверу
ssh user@192.168.1.72
cd ~/forgejo-runner

# Создайте .env файл
cp .env.example .env
nano .env  # Укажите FORGEJO_INSTANCE_URL и FORGEJO_RUNNER_TOKEN

# Запустите runner
docker-compose up -d --build
```

**Получение токена для Forgejo:**

1. Откройте ваш Forgejo instance
2. Settings → Actions → Runners
3. Нажмите "Create new Runner"
4. Скопируйте токен регистрации
5. Добавьте в `.env` файл

**Проверка:**
- Settings → Actions → Runners - runner должен быть **Online**
- Проверьте логи: `docker-compose logs -f`

**Преимущества Forgejo runner:**
- ✅ В **8-10 раз меньше** размер образа
- ✅ В **2-3 раза меньше** потребление RAM
- ✅ В **3-4 раза быстрее** запуск
- ✅ Идеально для **множественных runners** на одном сервере
- ✅ **Self-hosted** - полный контроль над данными

## 🔄 Workflow Deploy

Триггеры:
- Push в ветку `main`
- Закрытие одобренного Pull Request в `main`

Шаги:
1. Checkout кода
2. Создание директории для данных
3. Остановка старых контейнеров
4. Сборка новых образов (без кэша)
5. Запуск контейнеров
6. Проверка здоровья (health checks)
7. Очистка старых образов

## 🛠 Ручное развертывание

Если нужно развернуть вручную на сервере:

```bash
# На сервере в директории проекта
./deploy.sh
```

Или используя docker-compose:

```bash
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build
```

## 📦 Production конфигурация

Файл: `docker-compose.prod.yml`

Отличия от dev версии:
- Использует IP сервера (192.168.1.72) вместо localhost
- Без volume монтирования исходников (для production стабильности)
- Healthchecks для проверки готовности сервисов
- Префикс контейнеров: `*-prod`

## 🌐 Доступ к приложению

После успешного развертывания:

- **Frontend**: http://192.168.1.72:3000
- **Backend API**: http://192.168.1.72:8000
- **API Docs**: http://192.168.1.72:8000/docs
- **Health Check**: http://192.168.1.72:8000/health

## 🔍 Мониторинг

### Просмотр логов deployment

На сервере:

```bash
cd ~/recording_life_system  # или где расположен проект
docker-compose -f docker-compose.prod.yml logs -f
```

### Проверка статуса контейнеров

```bash
docker-compose -f docker-compose.prod.yml ps
```

### Проверка здоровья приложения

```bash
# Backend
curl http://192.168.1.72:8000/health

# Frontend
curl -I http://192.168.1.72:3000
```

## 🐛 Troubleshooting

### Workflow не запускается

1. Проверьте статус runner: Settings → Actions → Runners
2. Убедитесь что runner со статусом "Idle" (зеленый)
3. Проверьте логи runner: `docker-compose logs -f github-runner`

### Deployment failed

1. Проверьте логи в Actions на GitHub
2. Проверьте логи runner на сервере
3. Проверьте доступность портов 3000 и 8000
4. Убедитесь что Docker работает на сервере

### Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose -f docker-compose.prod.yml logs

# Проверьте образы
docker images | grep todo

# Пересоберите образы
docker-compose -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.prod.yml up -d
```

## 🔐 Безопасность

- ✅ Runner изолирован в Docker контейнере
- ✅ Токены хранятся в `.env` (не в Git)
- ✅ Healthchecks для проверки работоспособности
- ✅ Автоматическая очистка старых образов

### Рекомендации:

1. Регулярно обновляйте runner и Docker
2. Используйте GitHub Secrets для чувствительных данных
3. Ограничьте доступ к серверу
4. Настройте мониторинг и алертинг

## 📚 Дополнительные возможности

### Уведомления (Будущее)

Можно добавить уведомления в Telegram/Slack при успешном/неуспешном деплое.

### Тестирование (Будущее)

Добавить шаги для запуска тестов перед деплоем:
- Backend: pytest
- Frontend: vitest
- Линтеры: flake8, eslint

### Rollback (Будущее)

Добавить возможность отката к предыдущей версии при ошибках.

---

## 📖 Полезные ссылки

- [Подробная инструкция по настройке runner](.github/runner/RUNNER_SETUP.md)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Self-hosted runners](https://docs.github.com/en/actions/hosting-your-own-runners)

---

Если возникли вопросы, создайте Issue в репозитории.
