# Быстрый старт после обновления схемы БД

## Для локальной разработки

```bash
# 1. Удалить старую БД
rm -f data/tasks.db

# 2. Запустить бэкенд (БД создастся автоматически)
cd backend
source .venv/bin/activate  # или активируйте вашу виртуальную среду
uvicorn app.main:app --reload
```

## С использованием Docker

```bash
# 1. Остановить контейнеры
docker-compose down

# 2. Удалить volume с БД
docker volume rm recording_life_system_tasks_data

# 3. Запустить заново
docker-compose up -d
```

## Или используйте скрипт

```bash
cd backend
./reset_db.sh
```

## Что изменилось?

### Ключевые изменения в UI:
- **Идентификатор** теперь отображается как `TASK-123` (вместо `#-номер`)
- **Приоритет** - текстовый: Critical, High, Medium, Low, Lowest (вместо чисел)
- **Новые поля** в форме создания задачи (раскрываются по кнопке "Показать дополнительные поля"):
  - Сроки: дата начала, дедлайн, оценка
  - Контекст: место, окружение, требования к сети
  - Классификация: теги, компоненты
  - Рутинность: повторяемые задачи

### Сортировка задач:
Теперь задачи сортируются по приоритету:
1. Critical (🔴)
2. High (🟠)
3. Medium (🟡)
4. Low (🟢)
5. Lowest (⚪)

А не по числовому значению, как было раньше.

## Совместимость с VoskDemoMicroCheck

API обратно совместимо, **но** поле `priority` теперь возвращает строку вместо числа.

Если VoskDemoMicroCheck ожидает числовой priority, добавьте маппинг:

```python
PRIORITY_MAPPING = {
    "Critical": 1,
    "High": 2,
    "Medium": 3,
    "Low": 4,
    "Lowest": 5
}

priority_number = PRIORITY_MAPPING.get(task["priority"], 3)
```

## Проблемы?

Если что-то не работает:
1. Убедитесь, что старая БД удалена
2. Проверьте логи бэкенда
3. Проверьте, что фронтенд пересобрался

```bash
# Пересборка фронтенда
cd frontend
npm install
npm run build
```
